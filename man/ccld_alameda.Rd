% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ccld_alameda.R
\name{ccld_alameda}
\alias{ccld_alameda}
\title{Live Alameda snapshot for a child-care facility type}
\usage{
ccld_alameda(type, cache = TRUE)
}
\arguments{
\item{type}{One of \code{"large_fccs"}, \code{"infant_centers"},
\code{"school_age_centers"}, \code{"preschools"}, or
\code{"single_licensed_centers"}. Partial matching via \code{\link[=match.arg]{match.arg()}} is
supported.}

\item{cache}{Logical value (default \code{TRUE}) controlling whether the on-disk
response cache is used.}
}
\value{
A 13-column slim tibble, matching \code{\link[=ccld_verify]{ccld_verify()}}. Search responses
do not include closure dates, so \code{date_closed} is \code{NA}.
}
\description{
Pulls the current set of Alameda County child-care facilities of the given
type from the live CCLD Transparency API. Internally, the function walks the
17 Alameda cities to work around the API's 250-result per-call cap, unions
the rows, and deduplicates by \code{facility_number}.
}
\details{
Two facility types are deliberately unsupported: \code{"small_fccs"} because the
API blocks \code{facType = 0} searches, and \code{"centers"} because the API's
\code{facType = 845} bucket only contains a few legacy records statewide. For the
center workflow, use \code{"preschools"} (\code{facType = 850}).

If any city query reports 250 or more results, \code{ccld_alameda()} warns that
the API cap may have hidden additional facilities. The returned rows are
deduplicated by \code{facility_number}.
}
\examples{
\dontrun{
ccld_alameda("preschools")
ccld_alameda("large_fccs")
}
}
\seealso{
\code{\link[=ccld_verify]{ccld_verify()}} for the shared slim return schema.
}
\concept{Alameda snapshots}
