% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ccld_census_geographies.R
\name{ccld_add_census_geographies}
\alias{ccld_add_census_geographies}
\title{Add Census geographies to CCLD facility rows}
\usage{
ccld_add_census_geographies(
  data,
  address_col = "street_address",
  city_col = "city",
  state_col = NULL,
  zip_col = "zip",
  default_state = "CA",
  benchmark = "Public_AR_Current",
  vintage = "Current_Current",
  layers = "all",
  cache = TRUE
)
}
\arguments{
\item{data}{A data frame containing facility address columns.}

\item{address_col}{Name of the street address column. Defaults to
\code{"street_address"}.}

\item{city_col}{Name of the city column. Defaults to \code{"city"}.}

\item{state_col}{Optional name of the state column. When \code{NULL}, missing, or
blank, \code{default_state} is used.}

\item{zip_col}{Name of the ZIP code column. Defaults to \code{"zip"}.}

\item{default_state}{State abbreviation used when \code{state_col} is absent or
blank. Defaults to \code{"CA"}.}

\item{benchmark}{Census Geocoder benchmark. Defaults to
\code{"Public_AR_Current"}.}

\item{vintage}{Census Geocoder vintage. Defaults to \code{"Current_Current"}.}

\item{layers}{Census geography layers to request. Defaults to \code{"all"} so
ZCTA and school district layers are included when available.}

\item{cache}{Logical value (default \code{TRUE}) controlling whether the on-disk
response cache is used.}
}
\value{
A tibble containing the input columns plus Census geocoding and
geography columns.
}
\description{
Geocodes facility address columns with the U.S. Census Geocoder and appends
Census geography identifiers, including tract, block, block group, ZCTA,
place, county subdivision, urban area, legislative districts, and school
district fields when the geocoder returns them.
}
\details{
\code{ccld_add_census_geographies()} works with rows returned by
\code{\link[=ccld_verify]{ccld_verify()}}, \code{\link[=ccld_facility]{ccld_facility()}}, and \code{\link[=ccld_alameda]{ccld_alameda()}}. Inputs are returned
in the same order, and unmatched or ungeocodable rows are kept with
\code{geocode_status = "unmatched"} or \code{"not_geocoded"}.

The Census Geocoder does not require an API key. Results are cached in the
package cache using the same cache controls as the CCLD API helpers.
}
\examples{
\dontrun{
ccld_alameda("preschools") |>
  ccld_add_census_geographies()
}
}
\concept{geography helpers}
